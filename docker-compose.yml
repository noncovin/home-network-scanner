services:
  home-network-scanner:
    build: .
    container_name: home-network-scanner
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./app/templates:/app/app/templates
      - ./scanner_data:/app/scanner_data
    restart: unless-stopped
